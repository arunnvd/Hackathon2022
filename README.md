# Hackathon2022
Requires python modules:
  - open-cv : reading image
       ```sh
       pip install opencv-python
       ```
  - pyzbar : for decoding qrcode
       ```sh
       pip install pyzbar
       ```
